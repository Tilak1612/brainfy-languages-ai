# Add your Anthropic API key here to enable the real AI tutor (Voice screen).
# Copy this file to `.env` and fill in the key. The key is used ONLY by the
# Vite dev server (server-side); it is never bundled into the browser.
# Without a key, the app falls back to the scripted tutor and still works.
ANTHROPIC_API_KEY=
